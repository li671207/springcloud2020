server:
  port: 9002
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #server-addr: localhost:8848 #配置nacos地址
        server-addr: 192.168.1.106:1111 #nginx转发nacos集群

management:
  endpoints:
    web:
      exposure:
        include: '*'
