server:
  port: 9004
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #nacos注册中心
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'
